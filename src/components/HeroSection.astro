---
import type { Locale } from "../data/site";
import { contactInfo, locales } from "../data/site";

interface Props {
  lang: Locale;
}

const { lang } = Astro.props;
const content = locales[lang];
---

<section class="hero">
  <div class="shell hero-grid">
    <div class="hero-copy">
      <div class="hero-brand">
        <img src="/images/logo.png" alt="3P Kémény" class="hero-logo" width="160" height="80" />
        <p class="eyebrow">{content.hero.eyebrow}</p>
      </div>
      <h1>{content.hero.title}</h1>
      <p class="hero-text">{content.hero.text}</p>
      <div class="hero-actions">
        <a class="button button-primary" href="#contact">{content.hero.primaryCta}</a>
        <a class="button button-secondary" href={contactInfo.phoneTel}>{content.hero.phoneCta}</a>
        <a class="button button-tertiary" href="#gallery">{content.hero.galleryCta}</a>
      </div>
    </div>
    <div class="hero-visual">
      <img
        src="/images/hero.jpg"
        alt="Industrial chimney installation with crane — 3P Kémény"
        width="600"
        height="800"
      />
    </div>
  </div>
</section>
